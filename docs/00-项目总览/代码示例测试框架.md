# BufferCache 项目代码示例测试框架

## 📋 目录

- [1. 概述](#1-概述)
- [2. 测试框架选择](#2-测试框架选择)
- [3. 测试结构](#3-测试结构)
- [4. 测试示例](#4-测试示例)
- [5. CI/CD集成](#5-cicd集成)
- [6. 实施计划](#6-实施计划)

---

## 1. 概述

### 1.1 目标

为所有代码示例建立测试框架，确保：
- 代码示例可运行
- 代码示例正确性
- 代码示例性能
- 代码示例文档完整性

### 1.2 原则

- **全面覆盖**：所有代码示例都应包含测试
- **自动化测试**：使用自动化测试框架
- **持续集成**：集成到CI/CD流程
- **文档同步**：测试与文档同步更新

---

## 2. 测试框架选择

### 2.1 语言对应测试框架

| 语言 | 测试框架 | 断言库 |
|------|---------|--------|
| **Python** | pytest | pytest内置 |
| **Java** | JUnit 5 | AssertJ |
| **Go** | testing | 标准库 |
| **JavaScript** | Jest | Jest内置 |
| **C** | Unity | Unity |

### 2.2 推荐方案

**Python示例**：使用pytest
**Java示例**：使用JUnit 5
**Go示例**：使用标准testing包
**JavaScript示例**：使用Jest

---

## 3. 测试结构

### 3.1 目录结构

```text
docs/
├── examples/              # 代码示例目录
│   ├── python/
│   │   ├── lru_cache.py
│   │   └── test_lru_cache.py
│   ├── java/
│   │   ├── LRUCache.java
│   │   └── LRUCacheTest.java
│   └── go/
│       ├── lru_cache.go
│       └── lru_cache_test.go
└── tests/                 # 集成测试目录
    ├── integration/
    └── performance/
```

### 3.2 测试文件命名

- **Python**：`test_*.py`
- **Java**：`*Test.java`
- **Go**：`*_test.go`
- **JavaScript**：`*.test.js`

---

## 4. 测试示例

### 4.1 Python测试示例

**examples/python/test_lru_cache.py**：

```python
import pytest
from lru_cache import LRUCache

class TestLRUCache:
    """LRU缓存测试类"""

    def test_basic_operations(self):
        """测试基本操作"""
        cache = LRUCache(2)

        # 测试插入
        cache.put(1, 1)
        cache.put(2, 2)
        assert cache.get(1) == 1

        # 测试淘汰
        cache.put(3, 3)
        assert cache.get(2) == -1  # 2被淘汰

    def test_capacity(self):
        """测试容量限制"""
        cache = LRUCache(2)
        cache.put(1, 1)
        cache.put(2, 2)
        cache.put(3, 3)

        assert cache.get(1) == -1  # 1被淘汰
        assert cache.get(2) == 2
        assert cache.get(3) == 3

    def test_update_existing_key(self):
        """测试更新已存在的键"""
        cache = LRUCache(2)
        cache.put(1, 1)
        cache.put(1, 2)

        assert cache.get(1) == 2
```

### 4.2 Java测试示例

**examples/java/LRUCacheTest.java**：

```java
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeEach;
import static org.junit.jupiter.api.Assertions.*;

public class LRUCacheTest {
    private LRUCache cache;

    @BeforeEach
    void setUp() {
        cache = new LRUCache(2);
    }

    @Test
    void testBasicOperations() {
        cache.put(1, 1);
        cache.put(2, 2);
        assertEquals(1, cache.get(1));
    }

    @Test
    void testEviction() {
        cache.put(1, 1);
        cache.put(2, 2);
        cache.put(3, 3);
        assertEquals(-1, cache.get(1)); // 1被淘汰
    }
}
```

### 4.3 Go测试示例

**examples/go/lru_cache_test.go**：

```go
package main

import (
    "testing"
)

func TestLRUCache(t *testing.T) {
    cache := NewLRUCache(2)

    cache.Put(1, 1)
    cache.Put(2, 2)

    if val := cache.Get(1); val != 1 {
        t.Errorf("Expected 1, got %d", val)
    }

    cache.Put(3, 3)
    if val := cache.Get(2); val != -1 {
        t.Errorf("Expected -1 (evicted), got %d", val)
    }
}
```

---

## 5. CI/CD集成

### 5.1 GitHub Actions工作流

**.github/workflows/test-examples.yml**：

```yaml
name: Test Examples

on:
  push:
    branches:
      - main
    paths:
      - 'examples/**'
  pull_request:
    branches:
      - main
    paths:
      - 'examples/**'

jobs:
  test-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: pip install pytest
      - name: Run tests
        run: pytest examples/python/

  test-java:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '17'
      - name: Run tests
        run: |
          cd examples/java
          ./gradlew test

  test-go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      - name: Run tests
        run: go test ./examples/go/...
```

---

## 6. 实施计划

### 6.1 第一阶段（2周）

- [ ] 建立测试框架结构
- [ ] 为Python示例添加测试
- [ ] 配置CI/CD流程
- [ ] 测试自动化流程

### 6.2 第二阶段（2周）

- [ ] 为Java示例添加测试
- [ ] 为Go示例添加测试
- [ ] 为JavaScript示例添加测试
- [ ] 测试覆盖率达到80%

### 6.3 第三阶段（1周）

- [ ] 添加性能测试
- [ ] 添加集成测试
- [ ] 完善测试文档
- [ ] 正式上线

---

## 7. 测试覆盖率目标

| 语言 | 当前覆盖率 | 目标覆盖率 |
|------|-----------|-----------|
| Python | 0% | 80% |
| Java | 0% | 80% |
| Go | 0% | 80% |
| JavaScript | 0% | 80% |

---

## 8. 参考资源

### 8.1 测试框架文档

- [pytest文档](https://docs.pytest.org/)
- [JUnit 5文档](https://junit.org/junit5/)
- [Go testing文档](https://golang.org/pkg/testing/)
- [Jest文档](https://jestjs.io/)

---

**文档版本**：v1.0
**创建日期**：2025-01
**最后更新**：2025-01
**状态**：✅ 完成
