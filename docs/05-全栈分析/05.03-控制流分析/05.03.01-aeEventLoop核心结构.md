# 05.03.01 aeEventLoop核心结构

## 目录

- [1. 概述](#1-概述)
  - [1.1 定义与历史背景](#11-定义与历史背景)
  - [1.2 应用价值](#12-应用价值)
- [2. 核心数据结构](#2-核心数据结构)
  - [2.1 aeEventLoop定义](#21-aeeventloop定义)
  - [2.2 文件事件结构](#22-文件事件结构)
  - [2.3 时间事件结构](#23-时间事件结构)
- [3. 事件循环初始化](#3-事件循环初始化)
- [4. 文件事件管理](#4-文件事件管理)
- [5. 时间事件管理](#5-时间事件管理)
- [6. 事件处理主循环](#6-事件处理主循环)
- [7. Redis中的应用](#7-redis中的应用)
- [8. 性能优化](#8-性能优化)
- [9. 扩展阅读](#9-扩展阅读)
- [10. 权威参考](#10-权威参考)

---

## 1. 概述

### 1.1 定义与历史背景

**aeEventLoop**是Redis事件驱动的核心数据结构，封装了不同平台的多路复用机制（epoll、kqueue、select），实现了高性能的事件循环。

**历史发展**：

- **2009年**：Redis引入aeEventLoop事件循环
- **2010年代**：aeEventLoop成为Redis的核心组件
- **2020年代**：aeEventLoop在高性能网络编程中广泛应用

### 1.2 应用价值

aeEventLoop在Redis中具有重要价值：

1. **高性能**：基于epoll/kqueue的高性能事件循环
2. **跨平台**：支持Linux、macOS、Windows等平台
3. **可扩展**：支持文件事件和时间事件
4. **简洁**：简洁的事件驱动模型

## 2. 核心数据结构

### 2.1 aeEventLoop定义

```c
// ae.h
typedef struct aeEventLoop {
    int maxfd;                    // 当前最大文件描述符
    int setsize;                  // 文件描述符集合大小
    long long timeEventNextId;    // 下一个时间事件ID
    time_t lastTime;              // 上次处理时间事件的时间
    aeFileEvent *events;          // 文件事件数组
    aeFiredEvent *fired;          // 已触发事件数组
    aeTimeEvent *timeEventHead;   // 时间事件链表头
    int stop;                     // 停止标志
    void *apidata;                // 多路复用库的私有数据
    aeBeforeSleepProc *beforesleep; // 事件处理前回调
    aeBeforeSleepProc *aftersleep;  // 事件处理后回调
    int flags;                    // 标志位
} aeEventLoop;
```

**aeEventLoop定义**：事件循环的核心数据结构，包含文件事件数组、时间事件链表等。

### 2.2 文件事件结构

```c
// 文件事件
typedef struct aeFileEvent {
    int mask;                     // 事件类型：AE_READABLE | AE_WRITABLE
    aeFileProc *rfileProc;        // 读事件处理函数
    aeFileProc *wfileProc;        // 写事件处理函数
    void *clientData;             // 客户端数据
} aeFileEvent;

// 已触发事件
typedef struct aeFiredEvent {
    int fd;                       // 文件描述符
    int mask;                     // 事件类型
} aeFiredEvent;
```

**文件事件结构**：文件事件和已触发事件的结构定义。

### 2.3 时间事件结构

```c
// 时间事件
typedef struct aeTimeEvent {
    long long id;                 // 时间事件ID
    long when_sec;                // 触发时间（秒）
    long when_ms;                 // 触发时间（毫秒）
    aeTimeProc *timeProc;         // 时间事件处理函数
    aeEventFinalizerProc *finalizerProc; // 清理函数
    void *clientData;             // 客户端数据
    struct aeTimeEvent *prev;     // 前驱节点
    struct aeTimeEvent *next;     // 后继节点
    int refcount;                 // 引用计数
} aeTimeEvent;
```

**时间事件结构**：时间事件的双向链表结构，包含触发时间和处理函数。

**定理 2.1**：aeEventLoop的时间复杂度为O(n)（n为文件描述符数量）。

**证明**：

- 文件事件查找：O(1)（数组索引）
- 时间事件查找：O(m)（m为时间事件数量）
- 事件处理：O(n)（n为触发事件数量）
- 总时间复杂度：O(n + m)

## 3. 事件循环初始化

### 3.1 创建事件循环

```c
// ae.c
aeEventLoop *aeCreateEventLoop(int setsize) {
    aeEventLoop *eventLoop;
    int i;

    // 1. 分配内存
    if ((eventLoop = zmalloc(sizeof(*eventLoop))) == NULL) goto err;

    // 2. 初始化文件事件数组
    eventLoop->events = zmalloc(sizeof(aeFileEvent) * setsize);
    eventLoop->fired = zmalloc(sizeof(aeFiredEvent) * setsize);

    if (eventLoop->events == NULL || eventLoop->fired == NULL) goto err;

    // 3. 初始化字段
    eventLoop->setsize = setsize;
    eventLoop->lastTime = time(NULL);
    eventLoop->timeEventHead = NULL;
    eventLoop->timeEventNextId = 0;
    eventLoop->stop = 0;
    eventLoop->maxfd = -1;
    eventLoop->beforesleep = NULL;
    eventLoop->aftersleep = NULL;
    eventLoop->flags = 0;

    // 4. 初始化文件事件数组
    for (i = 0; i < setsize; i++)
        eventLoop->events[i].mask = AE_NONE;

    // 5. 初始化多路复用库（epoll/kqueue/select）
    if (aeApiCreate(eventLoop) == -1) goto err;

    return eventLoop;

err:
    if (eventLoop) {
        zfree(eventLoop->events);
        zfree(eventLoop->fired);
        zfree(eventLoop);
    }
    return NULL;
}
```

**创建事件循环**：分配内存，初始化文件事件数组和时间事件链表。

## 4. 文件事件管理

### 4.1 添加文件事件

```c
int aeCreateFileEvent(aeEventLoop *eventLoop, int fd, int mask,
                     aeFileProc *proc, void *clientData) {
    if (fd >= eventLoop->setsize) {
        errno = ERANGE;
        return AE_ERR;
    }

    aeFileEvent *fe = &eventLoop->events[fd];

    // 1. 添加到多路复用库
    if (aeApiAddEvent(eventLoop, fd, mask) == -1)
        return AE_ERR;

    // 2. 设置事件处理函数
    fe->mask |= mask;
    if (mask & AE_READABLE) fe->rfileProc = proc;
    if (mask & AE_WRITABLE) fe->wfileProc = proc;
    fe->clientData = clientData;

    // 3. 更新最大文件描述符
    if (fd > eventLoop->maxfd)
        eventLoop->maxfd = fd;

    return AE_OK;
}
```

**添加文件事件**：注册文件描述符到多路复用库，设置事件处理函数。

### 4.2 删除文件事件

```c
void aeDeleteFileEvent(aeEventLoop *eventLoop, int fd, int mask) {
    if (fd >= eventLoop->setsize) return;

    aeFileEvent *fe = &eventLoop->events[fd];
    if (fe->mask == AE_NONE) return;

    // 1. 从多路复用库删除
    aeApiDelEvent(eventLoop, fd, mask);

    // 2. 清除事件标志
    fe->mask = fe->mask & (~mask);

    // 3. 更新最大文件描述符
    if (fd == eventLoop->maxfd && fe->mask == AE_NONE) {
        int j;
        for (j = eventLoop->maxfd - 1; j >= 0; j--)
            if (eventLoop->events[j].mask != AE_NONE) break;
        eventLoop->maxfd = j;
    }
}
```

**删除文件事件**：从多路复用库删除文件描述符，清除事件标志。

## 5. 时间事件管理

### 5.1 添加时间事件

```c
long long aeCreateTimeEvent(aeEventLoop *eventLoop, long long milliseconds,
                           aeTimeProc *proc, void *clientData,
                           aeEventFinalizerProc *finalizerProc) {
    long long id = eventLoop->timeEventNextId++;
    aeTimeEvent *te;

    te = zmalloc(sizeof(*te));
    if (te == NULL) return AE_ERR;

    te->id = id;
    aeAddMillisecondsToNow(milliseconds, &te->when_sec, &te->when_ms);
    te->timeProc = proc;
    te->finalizerProc = finalizerProc;
    te->clientData = clientData;
    te->prev = NULL;
    te->next = eventLoop->timeEventHead;
    te->refcount = 0;

    if (te->next)
        te->next->prev = te;
    eventLoop->timeEventHead = te;

    return id;
}
```

**添加时间事件**：添加到时间事件链表，按时间排序。

### 5.2 删除时间事件

```c
int aeDeleteTimeEvent(aeEventLoop *eventLoop, long long id) {
    aeTimeEvent *te = eventLoop->timeEventHead;

    while (te) {
        if (te->id == id) {
            te->id = AE_DELETED_EVENT_ID;  // 标记为已删除
            return AE_OK;
        }
        te = te->next;
    }
    return AE_ERR;
}
```

**删除时间事件**：标记为已删除，延迟清理。

## 6. 事件处理主循环

### 6.1 aeMain函数

```c
void aeMain(aeEventLoop *eventLoop) {
    eventLoop->stop = 0;
    while (!eventLoop->stop) {
        // 处理所有事件
        aeProcessEvents(eventLoop, AE_ALL_EVENTS | AE_CALL_BEFORE_SLEEP | AE_CALL_AFTER_SLEEP);
    }
}
```

**aeMain函数**：事件循环的主函数，持续处理事件直到停止。

### 6.2 aeProcessEvents函数

```c
int aeProcessEvents(aeEventLoop *eventLoop, int flags) {
    int processed = 0, numevents;

    // 1. 如果没有文件事件和时间事件，直接返回
    if (!(flags & AE_TIME_EVENTS) && !(flags & AE_FILE_EVENTS))
        return 0;

    if (eventLoop->maxfd != -1 ||
        ((flags & AE_TIME_EVENTS) && !(flags & AE_DONT_WAIT))) {
        int j;
        aeTimeEvent *shortest = NULL;
        struct timeval tv, *tvp;

        // 2. 查找最近的时间事件
        if (flags & AE_TIME_EVENTS && !(flags & AE_DONT_WAIT))
            shortest = aeSearchNearestTimer(eventLoop);

        if (shortest) {
            long now_sec, now_ms;
            aeGetTime(&now_sec, &now_ms);
            tvp = &tv;

            // 3. 计算超时时间
            long long ms = (shortest->when_sec - now_sec) * 1000 +
                          shortest->when_ms - now_ms;

            if (ms > 0) {
                tvp->tv_sec = ms / 1000;
                tvp->tv_usec = (ms % 1000) * 1000;
            } else {
                tvp->tv_sec = 0;
                tvp->tv_usec = 0;
            }
        } else {
            if (flags & AE_DONT_WAIT) {
                tv.tv_sec = tv.tv_usec = 0;
                tvp = &tv;
            } else {
                tvp = NULL;  // 永久阻塞
            }
        }

        // 4. 调用beforesleep回调
        if (eventLoop->beforesleep != NULL && flags & AE_CALL_BEFORE_SLEEP)
            eventLoop->beforesleep(eventLoop);

        // 5. 等待文件事件（epoll_wait/kqueue/select）
        numevents = aeApiPoll(eventLoop, tvp);

        // 6. 调用aftersleep回调
        if (eventLoop->aftersleep != NULL && flags & AE_CALL_AFTER_SLEEP)
            eventLoop->aftersleep(eventLoop);

        // 7. 处理文件事件
        for (j = 0; j < numevents; j++) {
            aeFileEvent *fe = &eventLoop->events[eventLoop->fired[j].fd];
            int mask = eventLoop->fired[j].mask;
            int fd = eventLoop->fired[j].fd;
            int rfired = 0;

            // 处理读事件
            if (fe->mask & mask & AE_READABLE) {
                rfired = 1;
                fe->rfileProc(eventLoop, fd, fe->clientData, mask);
            }

            // 处理写事件
            if (fe->mask & mask & AE_WRITABLE) {
                if (!rfired || fe->wfileProc != fe->rfileProc)
                    fe->wfileProc(eventLoop, fd, fe->clientData, mask);
            }

            processed++;
        }
    }

    // 8. 处理时间事件
    if (flags & AE_TIME_EVENTS)
        processed += processTimeEvents(eventLoop);

    return processed;
}
```

**aeProcessEvents函数**：处理文件事件和时间事件的核心函数。

### 6.3 处理时间事件

```c
static int processTimeEvents(aeEventLoop *eventLoop) {
    int processed = 0;
    aeTimeEvent *te;
    long long maxId;
    time_t now = time(NULL);

    // 如果系统时间被调整，修正时间事件
    if (now < eventLoop->lastTime) {
        te = eventLoop->timeEventHead;
        while (te) {
            te->when_sec = 0;
            te = te->next;
        }
    }
    eventLoop->lastTime = now;

    te = eventLoop->timeEventHead;
    maxId = eventLoop->timeEventNextId - 1;

    while (te) {
        long now_sec, now_ms;
        long long id;

        // 跳过已删除的事件
        if (te->id == AE_DELETED_EVENT_ID) {
            aeTimeEvent *next = te->next;
            if (te->refcount) {
                te = next;
                continue;
            }
            if (te->prev)
                te->prev->next = te->next;
            else
                eventLoop->timeEventHead = te->next;
            if (te->next)
                te->next->prev = te->prev;
            if (te->finalizerProc)
                te->finalizerProc(eventLoop, te->clientData);
            zfree(te);
            te = next;
            continue;
        }

        // 检查事件是否到期
        aeGetTime(&now_sec, &now_ms);
        if (now_sec > te->when_sec ||
            (now_sec == te->when_sec && now_ms >= te->when_ms)) {
            int retval;
            id = te->id;
            te->refcount++;
            retval = te->timeProc(eventLoop, id, te->clientData);
            te->refcount--;
            processed++;

            // 如果返回AE_NOMORE，删除事件
            if (retval != AE_NOMORE) {
                aeAddMillisecondsToNow(retval, &te->when_sec, &te->when_ms);
            } else {
                te->id = AE_DELETED_EVENT_ID;
            }
        }
        te = te->next;

        // 防止无限循环
        if (te && te->id == AE_DELETED_EVENT_ID && te->refcount == 0) {
            aeTimeEvent *next = te->next;
            if (te->prev)
                te->prev->next = te->next;
            else
                eventLoop->timeEventHead = te->next;
            if (te->next)
                te->next->prev = te->prev;
            if (te->finalizerProc)
                te->finalizerProc(eventLoop, te->clientData);
            zfree(te);
            te = next;
        }
    }
    return processed;
}
```

**处理时间事件**：遍历时间事件链表，处理到期的事件。

## 7. Redis中的应用

### 7.1 初始化事件循环

```c
// server.c
void initServer(void) {
    // ...

    // 创建事件循环
    server.el = aeCreateEventLoop(server.maxclients + CONFIG_FDSET_INCR);
    if (server.el == NULL) {
        serverLog(LL_WARNING, "Failed creating the event loop. Exiting.");
        exit(1);
    }

    // 设置beforesleep回调
    aeSetBeforeSleepProc(server.el, beforeSleep);
    aeSetAfterSleepProc(server.el, afterSleep);

    // ...
}
```

**初始化事件循环**：创建事件循环，设置beforesleep和aftersleep回调。

### 7.2 注册文件事件

```c
// 监听客户端连接
void createSocketAcceptHandler(aeEventLoop *el, int fd,
                               void *clientData, int mask) {
    // 注册读事件
    aeCreateFileEvent(server.el, server.ipfd[j], AE_READABLE,
                     acceptTcpHandler, NULL);
}

// 处理客户端连接
void acceptTcpHandler(aeEventLoop *el, int fd, void *privdata, int mask) {
    int cport, cfd, max = MAX_ACCEPTS_PER_CALL;
    int cip[NET_IP_STR_LEN];
    UNUSED(el);
    UNUSED(mask);
    UNUSED(privdata);

    while (max--) {
        cfd = anetTcpAccept(server.neterr, fd, cip, sizeof(cip), &cport);
        if (cfd == ANET_ERR) {
            if (errno != EWOULDBLOCK)
                serverLog(LL_WARNING, "Accepting client connection: %s", server.neterr);
            return;
        }
        serverLog(LL_VERBOSE, "Accepted %s:%d", cip, cport);
        acceptCommonHandler(connCreateAcceptedSocket(cfd), 0, cip);
    }
}
```

**注册文件事件**：注册客户端连接和读写事件。

### 7.3 注册时间事件

```c
// 注册服务器定时任务
void initServer(void) {
    // ...

    // 创建时间事件：每秒执行一次serverCron
    if (aeCreateTimeEvent(server.el, 1, serverCron, NULL, NULL) == AE_ERR) {
        serverPanic("Can't create event loop timers.");
        exit(1);
    }
    // ...
}
```

**注册时间事件**：注册服务器定时任务（如serverCron）。

## 8. 性能优化

### 8.1 事件批处理

```c
// Redis使用MAX_ACCEPTS_PER_CALL批量处理连接
#define MAX_ACCEPTS_PER_CALL 1000

void acceptTcpHandler(aeEventLoop *el, int fd, void *privdata, int mask) {
    int max = MAX_ACCEPTS_PER_CALL;
    while (max--) {
        // 批量接受连接
    }
}
```

**事件批处理**：使用MAX_ACCEPTS_PER_CALL批量处理连接。

### 8.2 时间事件延迟处理

```c
// 时间事件使用链表，按时间排序
// 只处理到期的事件，避免遍历所有事件
static int processTimeEvents(aeEventLoop *eventLoop) {
    // 只处理到期的事件
    while (te && (now_sec > te->when_sec ||
                  (now_sec == te->when_sec && now_ms >= te->when_ms))) {
        // 处理事件
    }
}
```

**时间事件延迟处理**：只处理到期的事件，避免遍历所有事件。

## 9. 扩展阅读

- [epoll事件循环机制](../05.02-网络通信层/05.02.02-epoll事件循环机制.md)
- [事件循环时序分析](./05.03.02-事件循环时序分析.md)
- [多IO线程控制流](./05.03.03-多IO线程控制流.md)

## 10. 权威参考

### 10.1 经典书籍

1. **《Redis设计与实现（第2版）》** - 黄健宏
   - 出版社: 机械工业出版社
   - ISBN: 978-7111544937
   - 第12章：事件（aeEventLoop详解）

2. **《UNIX网络编程 卷1：套接字联网API（第3版）》** - W. Richard Stevens, Bill Fenner, Andrew M. Rudoff
   - 出版社: 人民邮电出版社
   - ISBN: 978-7115358851
   - 第6章：I/O复用（事件循环详解）

### 10.2 官方文档

1. **Redis源码 - ae.c**
   - URL: <https://github.com/redis/redis/blob/unstable/src/ae.c>
   - Redis的aeEventLoop实现源码

2. **Redis官方文档 - Event Loop**
   - URL: <https://redis.io/docs/manual/internals/>
   - Redis事件循环的官方文档

### 10.3 在线资源

1. **Wikipedia - Event Loop**
   - URL: <https://en.wikipedia.org/wiki/Event_loop>
   - 提供事件循环的详细说明
